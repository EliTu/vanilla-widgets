import generateElement from './generateElement';

describe('Testing generateElement function', () => {
   it('Should generate a widget with all the correct attributes, texts and child elements', () => {
       
   }); 
});